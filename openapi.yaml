openapi: 3.0.3
info:
  title: Demo Fresh Payments API
  version: 1.0.0
paths:
  /transactions:
    get:
      summary: List transactions
      responses:
        '200':
          description: OK
    post:
      summary: Create transaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '201':
          description: Created
